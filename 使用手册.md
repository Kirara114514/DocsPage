# Tech-Docs 文档管理系统 - 完整使用手册

> **目标读者**：完全不熟悉技术的用户  
> **配套AI**：此前完全没有上下文的新AI  
> **目标**：让任何人在任意环境中完整复现这套系统

---

## 一、系统概述

### 1.1 这是什么？

Tech-Docs 是一个**个人知识管理系统**，帮助你：
- 用 Markdown 格式编写技术文档
- 自动整理、分类、统计文档
- 通过网页界面浏览和搜索
- 支持 Git 版本控制，云端备份

### 1.2 系统架构（简化版）

```
┌─────────────────────────────────────────────────────────┐
│  你的操作                                               │
│  1. 在 Tech-Docs/ 目录下写 Markdown 文件                │
│  2. 提交到 Git 仓库                                     │
└────────────────────┬────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────┐
│  自动化流程                                             │
│  Git Push → Webhook → 服务器拉取 → 生成索引 → 更新网站  │
└────────────────────┬────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────┐
│  查看结果                                               │
│  打开浏览器 → 访问网站 → 看到最新的文档                 │
└─────────────────────────────────────────────────────────┘
```

### 1.3 核心组件说明

| 组件 | 位置 | 作用 |
|------|------|------|
| **文档库** | `Tech-Docs/` | 存放所有 Markdown 文档 |
| **索引生成器** | `generator-new.py` | 扫描文档并生成索引文件 |
| **索引文件** | `index.json` | 记录所有文档的元数据 |
| **前端页面** | `index.html`, `docs.html` | 展示文档的网页界面 |
| **前端资源** | `assets/css/`, `assets/js/` | 按共享层与页面层拆分样式、交互和动效逻辑 |
| **Webhook** | `webhook.php` | 接收 GitHub 更新通知 |

---

## 二、文档格式规范

### 2.1 文件格式要求

**必须使用 Markdown 格式**，文件扩展名为 `.md`

### 2.2 文档模板（标准格式）

每个文档必须包含以下结构：

```markdown
# 文档标题

## 摘要
这里是文档的核心内容摘要，简要说明这篇文章讲什么。

## 正文

### 背景
介绍这个问题的背景知识。

### 核心内容
详细的正文内容，可以有多级标题。

### 实现方案
具体的代码实现或操作步骤。

### 总结
归纳本文的重点和结论。

## 元数据
- **创建时间：** 2026-04-13 14:30
- **最后更新：** 2026-04-13 16:45
- **作者：** 吉良吉影
- **分类：** 编程范式
- **标签：** 技术文档，Unity，C#
- **来源：** （可选：原文链接）
- **状态：** （可选：已完成/进行中/待讨论）
- **优先级：** （可选：高/中/低）

---
*文档基于与吉良吉影的讨论，由小雅整理*
```

### 2.3 关键规则

#### 元数据部分必须严格遵守：

1. **元数据标题**：必须写 `## 元数据`
2. **字段格式**：`- **字段名：** 值`（注意是全角冒号 `：`）
3. **日期格式**：`YYYY-MM-DD HH:MM`（24小时制）
4. **标签格式**：多个标签用中文逗号 `，` 分隔

#### 支持的元数据字段：

| 字段名 | 必填 | 说明 |
|--------|------|------|
| 创建时间 | 是 | 文档首次创建时间 |
| 最后更新 | 否 | 文档最后修改时间 |
| 作者 | 是 | 文档作者 |
| 分类 | 是 | 所属大类（见下文分类列表） |
| 标签 | 是 | 关键词，便于搜索 |
| 来源 | 否 | 参考来源链接 |
| 状态 | 否 | 文档完成状态 |
| 优先级 | 否 | 文档重要程度 |

### 2.4 文档分类

文档必须归入以下分类之一：

- **编程范式** - C#、异步编程、响应式编程等
- **性能优化** - 内存优化、UI优化、算法优化等
- **架构设计** - ECS、系统设计、框架设计等
- **内存管理** - GC、内存池、资源释放等
- **资源管理** - 对象池、AssetBundle、资源加载等
- **物理系统** - 碰撞检测、刚体、物理模拟等
- **模板规范** - 系统模板、规范文档（不参与主页显示）
- **日常记录** - 零散想法、待讨论内容
- **历史记录** - 归档的历史讨论

---

## 三、文件目录结构

### 3.1 完整目录树

```
/www/wwwroot/yanglei.asia/          # 网站根目录
│
├── index.html                       # 首页
├── docs.html                        # 文档管理界面
├── doc.html                         # 文档阅读界面
├── assets/                          # 共享样式与页面脚本
│   ├── css/                         # base/home/docs/doc 样式
│   └── js/                          # core/pages 模块化脚本
├── generator-new.py                 # 索引生成器
├── webhook.php                      # Webhook接收器
├── webhook-processor.sh             # Webhook处理脚本
├── index.json                       # 生成的文档索引（自动）
│
└── Tech-Docs/                       # 文档库根目录（Git仓库）
    ├── README.md                    # 仓库说明
    ├── 模板规范/                    # 模板文件存放
    │   ├── 正式文档模板.md
    │   ├── 日常记录模板.md
    │   └── 历史记录模板.md
    │
    ├── 编程范式/                    # 编程相关文档
    │   ├── C#反射在Unity中的应用.md
    │   ├── 响应式编程/              # 子分类（支持嵌套）
    │   │   └── 响应式编程入门教程.md
    │   └── ...
    │
    ├── 性能优化/                    # 性能相关文档
    │   ├── GC优化/
    │   ├── UGUI/
    │   └── ...
    │
    ├── 架构设计/                    # 架构相关文档
    ├── 内存管理/                    # 内存相关文档
    ├── 资源管理/                    # 资源相关文档
    ├── 物理系统/                    # 物理相关文档
    ├── 日常记录/                    # 零散想法记录
    └── 历史记录/                    # 归档的历史文档
```

### 3.2 目录命名规则

1. **一级目录**：使用中文分類名称（如"编程范式"、"性能优化"）
2. **子目录**：可以创建二级目录（如"编程范式/响应式编程"）
3. **文件命名**：简短清晰，以 `.md` 结尾
4. **禁止使用的目录名**（会触发错误）：
   - `.git`（Git系统目录）
   - 以 `.` 开头的隐藏目录

---

## 四、日常操作流程

### 4.1 编写新文档

#### 步骤 1：选择存放位置

根据文档内容选择分类：
- C# 相关内容 → `Tech-Docs/编程范式/`
- 性能优化 → `Tech-Docs/性能优化/`
- 杂项想法 → `Tech-Docs/日常记录/`

#### 步骤 2：创建文件

1. 在选择的目录下创建新文件
2. 文件名示例：`Unity协程最佳实践.md`
3. 使用模板填写内容（见 2.2 节）

#### 步骤 3：填写元数据

必须检查所有必填字段：
- ✅ 创建时间（格式正确）
- ✅ 作者
- ✅ 分类（与目录匹配）
- ✅ 标签（至少一个）

#### 步骤 4：保存文件

保存为 UTF-8 编码（防止中文乱码）。

### 4.2 更新已有文档

1. 找到原文件
2. 修改内容
3. 更新元数据中的"最后更新"时间
4. 保存文件

### 4.3 提交到 Git 仓库（触发网站更新）

```bash
cd /www/wwwroot/yanglei.asia/Tech-Docs

# 查看修改的文件
git status

# 添加所有修改
git add .

# 提交修改（写明修改内容）
git commit -m "添加：Unity协程最佳实践文档"

# 推送到 GitHub（触发自动更新）
git push origin main
```

**推送后 1 分钟内**，网站会自动更新。

### 4.4 查看更新结果

1. 打开网站首页
2. 强制刷新（按 `Ctrl+F5` 或 `Ctrl+Shift+R`）
3. 查看新增的文档

---

## 五、自动化配置说明

### 5.1 自动化流程说明

整个系统支持**全自动更新**，流程如下：

```

你在本地修改文档
      ↓
执行 git push
      ↓
GitHub 发送 Webhook 通知
      ↓
服务器收到通知，自动拉取代码
      ↓
服务器自动生成 index.json
      ↓
网站立即显示最新内容
```

全程无需人工干预。

### 5.2 Webhook 配置方法（供AI参考）

如果需要在新的环境配置 Webhook，需要执行以下步骤：

#### 第一步：配置 GitHub

1. 进入 GitHub 仓库 → Settings → Webhooks
2. 点击 "Add webhook"
3. 填写参数：
   - **Payload URL**: `http://服务器IP:8080/webhook.php`
   - **Content type**: `application/json`
   - **Secret**: `KiraTechDocsUpdated`（必须与 webhook.php 中一致）
   - **Events**: 勾选 "Just the push event"
   - **SSL verification**: Disable（如使用 HTTP）
4. 点击 "Add webhook"

#### 第二步：配置服务器

1. 确保 `webhook.php` 文件存在且可访问
2. 确保 PHP-FPM 支持（已配置在 Nginx 中）
3. 添加 Cron 任务每分钟执行处理脚本：
   ```bash
   crontab -e
   # 添加以下行：
   * * * * * /www/wwwroot/yanglei.asia/webhook-processor.sh >> /www/wwwroot/yanglei.asia/webhook-cron.log 2>&1
   ```

#### 第三步：测试

1. 在 GitHub Webhook 页面点击 "Recent Deliveries"
2. 找一个记录点击 "Redeliver" 测试
3. 查看服务器日志确认执行成功

---

## 六、故障排查指南

### 6.1 问题诊断流程

当网站显示异常时，按以下顺序检查：

#### 检查 1：浏览器缓存

**现象**：网站显示旧内容，看不到最新文档  
**解决**：强制刷新浏览器（Ctrl+F5）

#### 检查 2：index.json 是否更新

```bash
# 查看最新生成时间
ls -la /www/wwwroot/yanglei.asia/index.json

# 查看 JSON 内容
head -50 /www/wwwroot/yanglei.asia/index.json
```

#### 检查 3：手动运行生成器

```bash
cd /www/wwwroot/yanglei.asia
python3 generator-new.py
```

观察输出是否有错误。

#### 检查 4：文件权限

```bash
# 确保 index.json 可被 Web 服务器读取
ls -la /www/wwwroot/yanglei.asia/index.json
# 应该是：-rw-r--r-- 1 www www

# 如果不是，修复权限
chown www:www /www/wwwroot/yanglei.asia/index.json
chmod 644 /www/wwwroot/yanglei.asia/index.json
```

### 6.2 常见问题

#### Q1：文档在文件系统里存在，但网站不显示

**可能原因**：
- JSON 索引未更新 → 运行 `generator-new.py`
- 浏览器缓存 → 强制刷新
- 文件编码错误 → 检查是否为 UTF-8
- 元数据格式错误 → 检查 `## 元数据` 格式

#### Q2：中文显示乱码

**解决**：确保 Markdown 文件保存为 UTF-8 编码。

#### Q3：WebHook 不工作

**检查步骤**：
1. 查看 GitHub Recent Deliveries 看是否有错误码
2. 查看服务器日志：`tail /www/wwwroot/yanglei.asia/webhook.log`
3. 查看 Cron 日志：`tail /www/wwwroot/yanglei.asia/webhook-cron.log`
4. 确保 webhook.php 可访问：`curl http://localhost:8080/webhook.php`

#### Q4：生成器报错 "utf-8 codec can't decode"

**解决**：文件编码不是 UTF-8。用文本编辑器打开后另存为 UTF-8 格式。

---

## 七、给AI助手的操作指令

如果你是AI助手，用户要求你管理这套系统，记住以下规则：

### 7.1 禁止做的事情

❌ 不要修改 `generator-new.py` 中的核心解析逻辑（除非用户明确要求）  
❌ 不要更改文档模板规范  
❌ 不要随意移动 Tech-Docs 目录下的文件结构  
❌ 不要干涉 Git 仓库内容（只遵守规格显示）

### 7.2 允许做的事情

✅ 修复网站显示 Bug  
✅ 重新生成 index.json  
✅ 修改 CSS/JS 提升用户体验  
✅ 清理临时文件和备份  
✅ 配置 Webhook 和自动化  
✅ 帮助用户编写文档并检查格式

### 7.3 关键文件位置

```
/www/wwwroot/yanglei.asia/
├── Tech-Docs/              # Git 仓库，文档存放地
├── generator-new.py        # 生成器脚本
├── assets/js/pages/        # 各页面逻辑
├── assets/js/core/         # 共享数据、Markdown、动效逻辑
├── assets/css/             # 共享与页面样式
├── index.json              # 生成的索引文件
├── webhook.php             # Webhook 接收器
└── webhook-processor.sh    # Webhook 处理器
```

### 7.4 常用维护命令

```bash
# 重新生成索引
cd /www/wwwroot/yanglei.asia && python3 generator-new.py

# 修复文件权限
chown www:www /www/wwwroot/yanglei.asia/index.json
chmod 644 /www/wwwroot/yanglei.asia/index.json

# 拉取最新代码（ webhook 故障时手动执行）
cd /www/wwwroot/yanglei.asia/Tech-Docs && git pull origin main

# 查看 Webhook 日志
tail -f /www/wwwroot/yanglei.asia/webhook.log

# 测试 Webhook
curl -X POST http://localhost:8080/webhook.php \
  -H "Content-Type: application/json" \
  -H "X-GitHub-Event: push" \
  -H "X-Hub-Signature-256: sha256=$(echo -n '{"ref":"refs/heads/main"}' | openssl dgst -sha256 -hmac 'KiraTechDocsUpdated' | cut -d' ' -f2)" \
  -d '{"ref":"refs/heads/main"}'
```

---

## 八、文档编写最佳实践

### 8.1 命名规范

- **文件名**：简洁描述内容，如 `UnityGC优化指南.md`
- **标题**：第一级 `# 标题` 应与文件名呼应
- **分类**：与存放目录一致

### 8.2 内容组织

- **摘要**：100字以内，说清这篇文章解决什么问题
- **正文**：分章节，每章一个小标题
- **代码块**：使用 Markdown 代码块包裹
  ```csharp
  // 示例代码
  void Start() { }
  ```
- **图片**：如有需要，放在同级目录的 `images/` 文件夹

### 8.3 版本控制

- **小修改**：直接提交，message 写清楚修改内容
- **大修改**：先创建分支，测试后再合并
- **删除文档**：Git 删除并提交，index.json 会自动更新

---

## 九、总结

### 核心要点

1. **写文档**：在 `Tech-Docs/分类/` 下新建 `.md` 文件
2. **填格式**：严格按模板填写元数据（全角冒号！）
3. **提交**：`git add . && git commit && git push`
4. **查看**：打开网站，强制刷新看结果

### 系统优势

- ✅ 纯 Markdown，不依赖特定编辑器
- ✅ Git 版本控制，不怕误删
- ✅ 自动更新，无需手动同步
- ✅ 全文搜索，快速定位
- ✅ 分类清晰，层级分明

---

**编写日期**：2026-04-13  
**版本**：v1.0  
**适用系统**：Tech-Docs v5.0
